[project]
name = "upi-split-pay"
version = "1.0.0"
dependencies = [
    "fastapi>=0.115.0",
    "pydantic>=2.8.0",
    "sqlalchemy>=2.0.0",
    "httpx>=0.27.0"
]

[tool.vercel]
entrypoint = "backend.app.main:app"
